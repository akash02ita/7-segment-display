{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: gcc-10 build active file",
            // "command": "/usr/bin/gcc-10",
            "command": "gcc",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                // "${fileDirname}/${fileBasenameNoExtension}",
                "${workspaceFolder}/out/${fileBasenameNoExtension}",
                "-I", // custom newn parameters to allow gcc to search for local #include
                "."
            ],
            "options": {
                // "cwd": "${fileDirname}"
                "cwd": "${workspaceFolder}" // this wil prevent issues such as file not found
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
